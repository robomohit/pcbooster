<UserControl x:Class="RogCustom.App.Views.CapabilitiesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <Style x:Key="CapCard" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource BackgroundCardBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderSubtleBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="16,14"/>
            <Setter Property="Margin" Value="0,0,10,10"/>
            <Setter Property="MinWidth" Value="180"/>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
    <StackPanel>
        <Border Background="{StaticResource ErrorBannerBrush}" Padding="14,10" Margin="0,0,0,16" CornerRadius="4"
                BorderBrush="{StaticResource AccentRedBrush}" BorderThickness="1,0,0,0"
                Visibility="{Binding LastError, Converter={StaticResource StringToVisibility}}">
            <TextBlock Foreground="{StaticResource ForegroundLightBrush}" TextWrapping="Wrap" Text="{Binding LastError}"/>
        </Border>

        <TextBlock Text="CAPABILITIES" FontSize="11" FontWeight="Bold" Foreground="{StaticResource ForegroundDimBrush}" Margin="0,0,0,4"/>
        <TextBlock Text="System feature availability" FontSize="16" FontWeight="Light" Foreground="{StaticResource ForegroundLightBrush}" Margin="0,0,0,20"/>

        <WrapPanel>
            <Border Style="{StaticResource CapCard}">
                <StackPanel>
                    <TextBlock Text="ADMINISTRATOR" FontSize="10" Foreground="{StaticResource ForegroundMutedBrush}" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding IsAdmin}" FontSize="18" FontWeight="Light" Foreground="{StaticResource ForegroundLightBrush}" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource CapCard}">
                <StackPanel>
                    <TextBlock Text="POWER PLAN CONTROL" FontSize="10" Foreground="{StaticResource ForegroundMutedBrush}" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding PowerPlanAvailable}" FontSize="18" FontWeight="Light" Foreground="{StaticResource ForegroundLightBrush}" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource CapCard}">
                <StackPanel>
                    <TextBlock Text="HARDWARE MONITOR" FontSize="10" Foreground="{StaticResource ForegroundMutedBrush}" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding MonitorAvailable}" FontSize="18" FontWeight="Light" Foreground="{StaticResource ForegroundLightBrush}" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource CapCard}">
                <StackPanel>
                    <TextBlock Text="GPU CONTROL" FontSize="10" Foreground="{StaticResource ForegroundMutedBrush}" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding GpuControlAvailable}" FontSize="18" FontWeight="Light" Foreground="{StaticResource ForegroundLightBrush}" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource CapCard}">
                <StackPanel>
                    <TextBlock Text="FAN CONTROL" FontSize="10" Foreground="{StaticResource ForegroundMutedBrush}" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding FanControlAvailable}" FontSize="18" FontWeight="Light" Foreground="{StaticResource ForegroundLightBrush}" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
        </WrapPanel>
    </StackPanel>
    </ScrollViewer>
</UserControl>
