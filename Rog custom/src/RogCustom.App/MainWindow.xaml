<Window x:Class="RogCustom.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="RogCustom Desktop Hybrid" Width="1200" Height="800"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True"
        Background="#030305"
        ResizeMode="CanResizeWithGrip">
    <Window.Resources>
        <DropShadowEffect x:Key="WindowShadow" BlurRadius="20" ShadowDepth="0" Opacity="0.5" Color="Black"/>
    </Window.Resources>

    <Border Background="#030305" CornerRadius="8"
            BorderBrush="#333333" BorderThickness="1"
            Effect="{StaticResource WindowShadow}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- ═══ CUSTOM TITLE BAR ═══ -->
            <Border Grid.Row="0" Background="Transparent"
                    CornerRadius="8,8,0,0" MouseLeftButtonDown="TitleBar_MouseDown">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0,0,0">
                        <TextBlock Text="ROG" FontSize="14" FontWeight="Bold" Foreground="#00D4FF"
                                   VerticalAlignment="Center"/>
                        <TextBlock Text="CUSTOM" FontSize="14" FontWeight="Light" Foreground="#FFFFFF"
                                   Margin="4,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,8,0">
                        <Button Content="—" Background="Transparent" Foreground="White" BorderThickness="0" Padding="12,4" FontSize="14" Click="Minimize_Click"/>
                        <Button Content="□" Background="Transparent" Foreground="White" BorderThickness="0" Padding="12,4" FontSize="14" Click="Maximize_Click"/>
                        <Button x:Name="CloseBtn" Content="✕" Background="Transparent" Foreground="White" BorderThickness="0" Padding="12,4" FontSize="12" Click="Close_Click"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- ═══ WEBVIEW CONTAINER ═══ -->
            <wv2:WebView2 Grid.Row="1" Name="webView" />
        </Grid>
    </Border>
</Window>
