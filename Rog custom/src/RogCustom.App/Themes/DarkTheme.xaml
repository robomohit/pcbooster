<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:RogCustom.App">

    <!-- Backgrounds -->
    <Color x:Key="BgDeep">#FF080810</Color>
    <Color x:Key="BgBase">#FF0D0D15</Color>
    <Color x:Key="BgCard">#FF151520</Color>
    <Color x:Key="BgCardHover">#FF1C1C2A</Color>
    <Color x:Key="BgSidebar">#FF0A0A12</Color>
    <Color x:Key="BgInput">#FF12121C</Color>

    <!-- Accent -->
    <Color x:Key="AccentCyan">#FF00D4FF</Color>
    <Color x:Key="AccentCyanDim">#FF007A94</Color>
    <Color x:Key="AccentCyanGlow">#4000D4FF</Color>
    <Color x:Key="AccentRed">#FFFF3344</Color>
    <Color x:Key="AccentOrange">#FFFF8800</Color>
    <Color x:Key="AccentGreen">#FF00CC66</Color>
    <Color x:Key="AccentTeal">#FF00BFA5</Color>

    <!-- Foreground -->
    <Color x:Key="FgPrimary">#FFE8E8F0</Color>
    <Color x:Key="FgSecondary">#FF8888A0</Color>
    <Color x:Key="FgMuted">#FF555570</Color>
    <Color x:Key="FgDisabled">#FF3A3A50</Color>

    <!-- Border -->
    <Color x:Key="BorderSubtle">#FF222230</Color>
    <Color x:Key="BorderActive">#FF00D4FF</Color>

    <!-- BRUSHES -->
    <SolidColorBrush x:Key="BackgroundDeepBrush"   Color="{StaticResource BgDeep}"/>
    <SolidColorBrush x:Key="BackgroundBaseBrush"    Color="{StaticResource BgBase}"/>
    <SolidColorBrush x:Key="BackgroundCardBrush"    Color="{StaticResource BgCard}"/>
    <SolidColorBrush x:Key="BackgroundMediumBrush"  Color="{StaticResource BgCard}"/>
    <SolidColorBrush x:Key="BackgroundSidebarBrush" Color="{StaticResource BgSidebar}"/>
    <SolidColorBrush x:Key="BackgroundInputBrush"   Color="{StaticResource BgInput}"/>

    <SolidColorBrush x:Key="AccentBrush"         Color="{StaticResource AccentCyan}"/>
    <SolidColorBrush x:Key="AccentDimBrush"       Color="{StaticResource AccentCyanDim}"/>
    <SolidColorBrush x:Key="AccentGlowBrush"      Color="{StaticResource AccentCyanGlow}"/>
    <SolidColorBrush x:Key="AccentRedBrush"       Color="{StaticResource AccentRed}"/>
    <SolidColorBrush x:Key="AccentOrangeBrush"    Color="{StaticResource AccentOrange}"/>
    <SolidColorBrush x:Key="AccentGreenBrush"     Color="{StaticResource AccentGreen}"/>

    <SolidColorBrush x:Key="ForegroundLightBrush" Color="{StaticResource FgPrimary}"/>
    <SolidColorBrush x:Key="ForegroundMutedBrush" Color="{StaticResource FgSecondary}"/>
    <SolidColorBrush x:Key="ForegroundDimBrush"   Color="{StaticResource FgMuted}"/>
    <SolidColorBrush x:Key="ForegroundDisabledBrush" Color="{StaticResource FgDisabled}"/>

    <SolidColorBrush x:Key="BorderSubtleBrush"    Color="{StaticResource BorderSubtle}"/>
    <SolidColorBrush x:Key="BorderActiveBrush"    Color="{StaticResource BorderActive}"/>

    <SolidColorBrush x:Key="ErrorBannerBrush"     Color="#CC331122"/>
    <SolidColorBrush x:Key="WarningBrush"          Color="{StaticResource AccentOrange}"/>

    <!-- Gradient Brushes for Progress Bars -->
    <LinearGradientBrush x:Key="CyanGradientBrush" StartPoint="0,0" EndPoint="1,0">
        <GradientStop Color="#FF005577" Offset="0"/>
        <GradientStop Color="#FF00D4FF" Offset="1"/>
    </LinearGradientBrush>
    <LinearGradientBrush x:Key="TealGradientBrush" StartPoint="0,0" EndPoint="1,0">
        <GradientStop Color="#FF004D40" Offset="0"/>
        <GradientStop Color="#FF00BFA5" Offset="1"/>
    </LinearGradientBrush>
    <LinearGradientBrush x:Key="RedGradientBrush" StartPoint="0,0" EndPoint="1,0">
        <GradientStop Color="#FF661122" Offset="0"/>
        <GradientStop Color="#FFFF3344" Offset="1"/>
    </LinearGradientBrush>
    <LinearGradientBrush x:Key="OrangeGradientBrush" StartPoint="0,0" EndPoint="1,0">
        <GradientStop Color="#FF664400" Offset="0"/>
        <GradientStop Color="#FFFF8800" Offset="1"/>
    </LinearGradientBrush>
    <LinearGradientBrush x:Key="GreenGradientBrush" StartPoint="0,0" EndPoint="1,0">
        <GradientStop Color="#FF004D2B" Offset="0"/>
        <GradientStop Color="#FF00CC66" Offset="1"/>
    </LinearGradientBrush>

    <!-- CONVERTERS -->
    <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
    <local:StringToVisibilityConverter x:Key="StringToVisibility"/>

    <!-- ═══ PROGRESS BAR — Armoury Crate Style ═══ -->
    <Style x:Key="AcProgressBar" TargetType="ProgressBar">
        <Setter Property="Height" Value="4"/>
        <Setter Property="Minimum" Value="0"/>
        <Setter Property="Maximum" Value="100"/>
        <Setter Property="Background" Value="#10FFFFFF"/>
        <Setter Property="Foreground" Value="{StaticResource CyanGradientBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ProgressBar">
                    <Grid>
                        <Border x:Name="PART_Track" Background="{TemplateBinding Background}"
                                CornerRadius="2" SnapsToDevicePixels="True"/>
                        <Border x:Name="PART_Indicator" HorizontalAlignment="Left"
                                Background="{TemplateBinding Foreground}"
                                CornerRadius="2" SnapsToDevicePixels="True"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="AcProgressBarTeal" TargetType="ProgressBar" BasedOn="{StaticResource AcProgressBar}">
        <Setter Property="Foreground" Value="{StaticResource TealGradientBrush}"/>
    </Style>
    <Style x:Key="AcProgressBarRed" TargetType="ProgressBar" BasedOn="{StaticResource AcProgressBar}">
        <Setter Property="Foreground" Value="{StaticResource RedGradientBrush}"/>
    </Style>
    <Style x:Key="AcProgressBarOrange" TargetType="ProgressBar" BasedOn="{StaticResource AcProgressBar}">
        <Setter Property="Foreground" Value="{StaticResource OrangeGradientBrush}"/>
    </Style>
    <Style x:Key="AcProgressBarGreen" TargetType="ProgressBar" BasedOn="{StaticResource AcProgressBar}">
        <Setter Property="Foreground" Value="{StaticResource GreenGradientBrush}"/>
    </Style>

    <!-- ═══ BUTTON STYLE — Base ═══ -->
    <Style x:Key="AcButton" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource BackgroundCardBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource ForegroundLightBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderSubtleBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="16,10"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bd" Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4" Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                            <Setter TargetName="bd" Property="Background">
                                <Setter.Value>
                                    <SolidColorBrush Color="{StaticResource BgCardHover}"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{StaticResource AccentDimBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{StaticResource ForegroundDisabledBrush}"/>
                            <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource ForegroundDisabledBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Mode Button -->
    <Style x:Key="AcModeButton" TargetType="Button" BasedOn="{StaticResource AcButton}">
        <Setter Property="MinWidth" Value="140"/>
        <Setter Property="MinHeight" Value="60"/>
        <Setter Property="Padding" Value="20,14"/>
        <Setter Property="FontSize" Value="13"/>
    </Style>

    <!-- Mode Button Active — Cyan glow border -->
    <Style x:Key="AcModeButtonActive" TargetType="Button">
        <Setter Property="MinWidth" Value="140"/>
        <Setter Property="MinHeight" Value="60"/>
        <Setter Property="Padding" Value="20,14"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bd" CornerRadius="4" Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True"
                            BorderThickness="1.5" BorderBrush="{StaticResource AccentBrush}">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#1800D4FF" Offset="0"/>
                                <GradientStop Color="#0800D4FF" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <Border.Effect>
                            <DropShadowEffect Color="#FF00D4FF" BlurRadius="12" ShadowDepth="0" Opacity="0.3"/>
                        </Border.Effect>
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Mode Button Active Turbo — Red glow -->
    <Style x:Key="AcModeButtonActiveTurbo" TargetType="Button">
        <Setter Property="MinWidth" Value="140"/>
        <Setter Property="MinHeight" Value="60"/>
        <Setter Property="Padding" Value="20,14"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{StaticResource AccentRedBrush}"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bd" CornerRadius="4" Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True"
                            BorderThickness="1.5" BorderBrush="{StaticResource AccentRedBrush}">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#18FF3344" Offset="0"/>
                                <GradientStop Color="#08FF3344" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <Border.Effect>
                            <DropShadowEffect Color="#FFFF3344" BlurRadius="12" ShadowDepth="0" Opacity="0.3"/>
                        </Border.Effect>
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Make AcButton the default -->
    <Style TargetType="Button" BasedOn="{StaticResource AcButton}"/>

    <!-- ═══ NAV BUTTON STYLE ═══ -->
    <Style x:Key="NavButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource ForegroundMutedBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="16,12"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontWeight" Value="Normal"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <Border x:Name="highlight" Width="3" HorizontalAlignment="Left"
                                Background="Transparent" CornerRadius="0,2,2,0"/>
                        <Border x:Name="bd" Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}" CornerRadius="0">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="{StaticResource ForegroundLightBrush}"/>
                            <Setter TargetName="bd" Property="Background">
                                <Setter.Value>
                                    <SolidColorBrush Color="#18FFFFFF"/>
                                </Setter.Value>
                            </Setter>
                            <Setter TargetName="highlight" Property="Background" Value="{StaticResource AccentDimBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
                            <Setter TargetName="highlight" Property="Background" Value="{StaticResource AccentBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Active Nav Button -->
    <Style x:Key="NavButtonActive" TargetType="Button">
        <Setter Property="Background" Value="#10FFFFFF"/>
        <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="16,12"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <Border Width="3" HorizontalAlignment="Left"
                                Background="{StaticResource AccentBrush}" CornerRadius="0,2,2,0"/>
                        <Border Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}" CornerRadius="0">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ═══ COMBOBOX / TEXTBOX ═══ -->
    <Style TargetType="ComboBox">
        <Setter Property="Background" Value="{StaticResource BackgroundInputBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource ForegroundLightBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderSubtleBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8,6"/>
    </Style>

    <Style TargetType="TextBox">
        <Setter Property="Background" Value="{StaticResource BackgroundInputBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource ForegroundLightBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderSubtleBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8,6"/>
        <Setter Property="CaretBrush" Value="{StaticResource ForegroundLightBrush}"/>
    </Style>

    <!-- ═══ SCROLLBAR ═══ -->
    <Style TargetType="ScrollBar">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Width" Value="6"/>
    </Style>

    <!-- ═══ DATAGRID ═══ -->
    <Style TargetType="DataGrid">
        <Setter Property="Background" Value="{StaticResource BackgroundCardBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource ForegroundLightBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderSubtleBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="GridLinesVisibility" Value="None"/>
        <Setter Property="RowBackground" Value="Transparent"/>
        <Setter Property="AlternatingRowBackground">
            <Setter.Value>
                <SolidColorBrush Color="#08FFFFFF"/>
            </Setter.Value>
        </Setter>
        <Setter Property="HeadersVisibility" Value="Column"/>
    </Style>

    <Style TargetType="DataGridColumnHeader">
        <Setter Property="Background">
            <Setter.Value>
                <SolidColorBrush Color="#FF14141E"/>
            </Setter.Value>
        </Setter>
        <Setter Property="Foreground" Value="{StaticResource ForegroundMutedBrush}"/>
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="10,8"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderSubtleBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
    </Style>

    <Style TargetType="DataGridCell">
        <Setter Property="Padding" Value="10,6"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Foreground" Value="{StaticResource ForegroundLightBrush}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridCell">
                    <Border Padding="{TemplateBinding Padding}" Background="Transparent">
                        <ContentPresenter VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="DataGridRow">
        <Setter Property="Background" Value="Transparent"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background">
                    <Setter.Value>
                        <SolidColorBrush Color="#10FFFFFF"/>
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>
